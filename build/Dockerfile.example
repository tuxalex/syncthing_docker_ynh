# VERSION 0.1
# DESCRIPTION:    Yunohost app in container
# TO_BUILD:       docker build -rm -t  .
# TO_RUN:         docker run -d --name=<app> -i -t -p <port>:<port> -v /PATHDATA/:<pathdata> -v /PATHCONFIG/:<pathconfig> IMAGENAME

FROM ubuntu-debootstrap:14.04
MAINTAINER : Alexan Andrieux <alexan.andrieux@gmail.com>

ENV DEBIAN_FRONTEND noninteractive

# RUN ... 

# WORKDIR ...

# USER ...

# EXPOSE ...

# VOLUME [...]

#ENTRYPOINT [...]
